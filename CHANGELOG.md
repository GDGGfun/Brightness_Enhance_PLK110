# 更新日志

本文件由模块的更新检查（`update.json` 的 `changelog` 字段）直接读取并展示在
KernelSU 管理器的更新弹窗中，因此**只写具体更新内容**，不写与版本迭代无关的信息。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [v1.9] - 2026-09-22

> 本次为第二版本号迭代（1.8 → 1.9）。**内容与本模块 v1.8.5 完全一致**，
> 仅更新版本号并调整更新检查的下载通道。

### 变更

- **版本号迭代**：v1.8.5 → v1.9，作为 1.9 系列的稳定基线版本。
- **更新下载接入国内加速**：模块内置的更新检查在下载安装包时走加速通道，
  国内网络环境下下载更稳定。
- **更新说明改为内嵌展示**：点「更新」时，更新说明会直接显示在弹窗内
  （此前指向一个网页，弹窗里会显示网页源码）。

### 功能（与 v1.8.5 相同）

- **调整自动亮度曲线**：抬高 10–100% 区间的整体亮度，解决原厂自动亮度偏暗、阅读困难
- **提高最大亮度至 2200nit**，降低激发亮度触发阈值，提升阳光下可读性
- **自动亮度可自动触底到硬件 1nit**：无光室内不再需要手动把亮度条拉到最低
- **移除导航应用亮度特调**：原机把地图类 App 放进「进应用降亮度」的 method
  （≥800nit 削 37.5%、≥2000nit 削 60%），使它们永远够不到激发亮度门槛；
  现已移出该 method 并加入 `global_brightness_limit` 豁免名单
- **WebUI**：在 KernelSU 管理器里点击模块的 WebUI 入口即可打开
  - 实时环境光传感器值（lux）
  - 实时屏幕亮度（nit / 系统原始值 / 亮度条百分比）
  - 完整自动亮度曲线（lux → nit）并标出当前工作点
  - 激发亮度状态 / 屏幕状态 / 亮度模式 / 面板上限 / app_list 版本

---

## [v1.8.5] - 2026-09-22

### 修复

- **移除导航应用亮度特调**：原机把地图类 App（高德 / 百度 / 腾讯 / 谷歌地图 / 位智）
  放在专门的「进应用降亮度」method 中，`<ratio mode='0'>0.0,0.375,0.6</ratio>`
  —— 即 ≥800nit 削 37.5%、≥2000nit 削 60%。而常规模式上限本身即 800nit，
  导致这些 App **永远跨不过激发亮度门槛**，阳光下明显偏暗。
  本版**整块删除该 method**，并把 5 个 App 加入 `global_brightness_limit` 豁免名单。

### 新增

- **WebUI**：在 KernelSU 管理器点击本模块的 WebUI 入口即可打开，首页实时显示
  - 当前**环境光传感器值**（lux）
  - 当前**屏幕亮度**（nit / 系统原始值 / 亮度条百分比）
  - 完整**自动亮度曲线（lux → nit）**，并标出**当前工作点**
    （红点＝实际亮度，绿点＝曲线目标），附 800nit 激发门槛参考线

  数据来自模块内 `bin/status.sh`（**只读**），2 秒自动刷新。

### 变更

- **挂载方式改为依赖 KernelSU 元模块**：不再在 `post-fs-data.sh` 中自行 `mount --bind`
  （那会与元模块的官方挂载抢同一路径，实测会让元模块 overlay 挂载报 `EINVAL`
  并中止整条流水线，连带其它模块不被挂载）。现在配置以 promoted-partition 形态提供，
  由元模块统一挂载；`post-mount.sh` 仅作兜底。
- **权限匹配改到安装时**：用官方
  `set_perm_recursive <dir> 0 0 0755 0644 u:object_r:vendor_configs_file:s0`
  在安装时一次性设好权限与 SELinux 上下文；**启动脚本零写入**。

---

## [v1.8.3] - 2026-09-21

### 功能

- **调整自动亮度曲线**：抬高 10–100% 区间的整体亮度，解决原厂自动亮度偏暗、阅读困难
- **提高最大亮度至 2200nit**，降低激发亮度触发阈值，提升阳光下可读性
- **自动亮度可自动触底到硬件 1nit**：无光室内不再需要手动把亮度条拉到最低
- **移除导航应用亮度特调**

---

各版本安装包可在 [Releases](https://github.com/GDGGfun/Brightness_Enhance_PLK110/releases) 页面下载。
